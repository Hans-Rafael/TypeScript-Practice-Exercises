{
  "compilerOptions": {
    "target": "ES6", // Especifica a qué versión de JavaScript se debe compilar. ES6 es una versión moderna y compatible.
    "module": "commonjs",
    "moduleResolution": "node",
    "strict": true, // Activa estrictas verificaciones de tipo para ayudar a escribir un código más seguro y consistente.
    "esModuleInterop": true, // Permite una mejor interoperabilidad con módulos ES6, facilitando el uso de librerías de JavaScript.
    "outDir": "./dist", // Especifica la carpeta donde se generarán los archivos JavaScript compilados.
    "experimentalDecorators":true // para evitar errores en nuevas versiones con decorador
  },
  "include": [
    "./**/*.ts"
  ] // Incluye todos los archivos `.ts` en el proyecto, desde cualquier subdirectorio.
}